# Use your computer's LAN IP when testing Expo Go on a physical phone.
EXPO_PUBLIC_API_URL=http://localhost:4000

# "demo" keeps the call lifecycle inside the app.
# "system" also opens the device phone app with tel: after the server accepts the call.
EXPO_PUBLIC_CALL_MODE=demo
